# OptiPhi


#WATHCDOG_TIMEOUT is the value, in seconds, for the  watchdog time; must be between 1 and 15
WATCHDOG_TIMEOUT = 3.0;

# LOGFILE is the specified location and name of the log file

LOGFILE = "/home/pi/log.log";


#This is the time to wait between sending bits, measured in microseconds
TIME_BETWEEN_BITS = 40000;


SENDER_OUTPUT_PIN = 17;

RECIEVER_INPUT_PIN = 4;

#Sequence of bits to send in between charachters
NEW_CHAR_SEQUENCE = [0, 0, 1, 1, 1]; 

